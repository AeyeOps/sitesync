## 1. Implementation
- [x] 1.1 Evaluate uv bundling support and decide whether it replaces or wraps the existing PyInstaller flow.
- [x] 1.2 Add a default Makefile help target that prints available commands.
- [x] 1.3 Implement `make install` to build a wheel and install it into `/opt/bin` (no sudo).
- [x] 1.4 Add `make lint` and `make standalone` targets aligned with the new bundling approach.
- [x] 1.5 Update `sitesync.spec` with required hidden imports for a clean bundle.
- [x] 1.6 Update README installation/build sections (no fix details) and CHANGELOG entry.
- [x] 1.7 Update or add tests/validation steps if needed.
